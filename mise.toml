# ================================================================
# Settings
# ================================================================
[settings]
auto_install = false


# ================================================================
# Tools
# ================================================================
[tools]

# Markdown
markdownlint-cli2 = "0.22"
"github:23prime/md-section-numbers-checker" = "latest"
"github:kevingimbel/mktoc" = "latest"

# GitHub Actions
actionlint = "1"
shellcheck = "0.11"
"github:zizmorcore/zizmor" = "1"

# Other tools
lefthook = "2"
cspell = "9"


# ================================================================
# Tasks
# See: https://mise.jdx.dev/tasks/toml-tasks.html
# ================================================================

# Global
[tasks.setup]
description = "Setup tools"
run = ["mise trust -yq", "mise install", "lefthook install"]
alias = "s"

[tasks.fix]
description = "Fix all issues"
depends = ["md-fix", "gh-fix"]
alias = "f"

[tasks.check]
description = "Check for all issues"
depends = ["md-check", "gh-check", "spell-check"]
alias = "c"

[tasks.fix-and-check]
run = [{ task = "fix" }, { task = "check" }]
description = "Fix all issues and check for issues"
alias = "fc"

# Markdown
[tasks.md-fix]
description = "Fix Markdown issues"
run = [
  "mktoc --min-depth 2 --max-depth 3 ./contents/*.md",
  "markdownlint-cli2 '**/*.md' --fix",
]
alias = "mf"

[tasks.md-check]
description = "Check for Markdown issues"
run = ["markdownlint-cli2 '**/*.md'", "mdsnc '**/*.md'"]
alias = "mc"

# GitHub Actions
[tasks.gh-fix]
description = "Fix GitHub Actions workflows"
run = "zizmor --fix=all ."
alias = "ghf"

[tasks.gh-check]
description = "Check GitHub Actions workflows"
run = [
  "actionlint .github/workflows/*.yml",
  "zizmor . --persona=pedantic --min-severity=medium",
]
alias = "ghc"

# Spell Check
[tasks.spell-check]
description = "Check spelling"
run = "cspell . --gitignore --verbose"
alias = "sc"
